apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: gp3
  annotations:
    argocd.argoproj.io/sync-wave: "3"
provisioner: ebs.csi.aws.com # use addon
volumeBindingMode: WaitForFirstConsumer
parameters:
  type: gp3
